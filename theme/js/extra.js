// disable behavior in base.js. it messes up how the side bar is affixed to the top when
// announcement is visible. doesn't seem to be needed at all.
function applyTopPadding() {}
$(window).off('resize');
